<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            Test berjalan di PostgreSQL, bukan SQLite in-memory bawaan Laravel.
            Alasannya: skema FinGoal memakai jsonb (calculation_snapshot) dan
            tipe enum, yang tidak didukung SQLite dan diam-diam jatuh jadi text.
            Kalau test berjalan di SQLite, migrasi bisa lolos semua test lalu
            gagal saat pertama dijalankan sungguhan — persis kegagalan yang
            membuat kita memilih PostgreSQL sejak awal.

            Konsekuensinya database fingoal_test harus dibuat sekali di tiap
            mesin (lihat CONTRIBUTING.md §9). Host, port, user, dan password
            diambil dari .env.
        -->
        <env name="DB_CONNECTION" value="pgsql"/>
        <env name="DB_DATABASE" value="fingoal_test"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
